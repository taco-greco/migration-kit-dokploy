APP_NAME=CHANGE-ME
DB_TYPE=none                  # set to postgres/mysql if this app has its own DB, or "none" if it only talks to a shared DB you're backing up separately
DB_CONTAINER=
DB_NAME=
DB_USER=
VOLUMES=(app-data)
