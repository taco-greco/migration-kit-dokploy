APP_NAME=CHANGE-ME            # e.g. intranet-tool, one backup.conf per real app
DB_TYPE=none                  # set to mysql/postgres if this app has its own DB, and fill in below
DB_CONTAINER=
DB_NAME=
DB_USER=
VOLUMES=(app-storage)
# Duplicate this whole templates/php-app folder per real internal app
# (e.g. templates/intranet-tool/, templates/timesheet-app/) rather than
# trying to reuse one generic folder for several different apps.
